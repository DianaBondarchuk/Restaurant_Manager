<Window x:Class="Restaurant_Manager.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Restaurant_Manager.ViewModels"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400">

    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" FontSize="14" />
        <TextBox Grid.Row="0" Margin="0,25,0,0"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Password:" Grid.Row="1" Margin="0,60,0,0" FontSize="14" />
        <PasswordBox Grid.Row="1" Margin="0,85,0,0" x:Name="PasswordBox"/>

        <Button Content="Login"
                Grid.Row="2"
                Margin="0,150,0,0"
                Width="100"
                Height="35"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"/>
    </Grid>
</Window>
