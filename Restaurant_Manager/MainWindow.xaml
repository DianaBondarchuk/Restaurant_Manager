<Window x:Class="Restaurant_Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant_Manager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="#007ACC" Height="50">
                <Button Content="Замовлення" Command="{Binding NavigateOrdersCommand}" Margin="10"/>
                <Button Content="Меню" Command="{Binding NavigateMenuCommand}" Margin="10"/>
                <Button Content="Користувачі" Command="{Binding NavigateUsersCommand}" Margin="10"/>
                <Button Content="Вихід" Command="{Binding LogoutCommand}" Margin="10"/>
            </StackPanel>

            
            <ContentControl x:Name="MainContent" Content="{Binding CurrentView}" />
        </DockPanel>

    </Grid>
</Window>
