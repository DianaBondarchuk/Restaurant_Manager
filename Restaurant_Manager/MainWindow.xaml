<Window x:Class="RestaurantManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant_Manager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="#007ACC" Height="50">
                <Button Content="Замовлення"  Margin="10"
                        Click="Button_Click" /> <!--Command="{Binding NavigateOrdersCommand}"-->
                <Button Content="Меню" Margin="10"
                         Click="Button_Click_1"/> <!--Command="{Binding NavigateMenuCommand}"-->
                <Button Content="Користувачі" Margin="10"
                        Click="Button_Click_2"/> <!--Command="{Binding NavigateUsersCommand}"-->
                <Button Content="Вихід"  Margin="10"
                        Click="Button_Click_3"/> <!--Command="{Binding LogoutCommand}"-->
            </StackPanel>

            
            <ContentControl x:Name="MainContent" Content="{Binding CurrentView}" />
        </DockPanel>

    </Grid>
</Window>
