<Window x:Class="Restaurant_Manager.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Restaurant_Manager.ViewModels"
        xmlns:local="clr-namespace:Restaurant_Manager.Views"
        mc:Ignorable="d"
        Title="Login" Height="250" Width="300">



    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>




    <TextBox Text="{Binding Username}" Margin="0,0,0,10">
        <TextBox.Style>
            <Style TargetType="TextBox">
                <Setter Property="Foreground" Value="Gray"/>
                <Setter Property="Text" Value="Username"/>
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Text" Value=""/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </TextBox.Style>
    </TextBox>



</Window>
