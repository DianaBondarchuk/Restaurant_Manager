<Window x:Class="Restaurant_Manager.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant_Manager.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Width="300">
            <TextBlock Text="Авторизація" FontSize="20"
                       Margin="0,0,0,20" HorizontalAlignment="Center"/>


            <TextBox x:Name="LoginBox" Text="{Binding Username}" Margin="0,0,0,10"/>
            <TextBlock Text="Логін"
               Foreground="Maroon"
               Margin="5,2,0,0"
               IsHitTestVisible="False"
               VerticalAlignment="Center"/>

            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>
            <Button Content="Увійти" Command="{Binding LoginCommand}" Width="100" HorizontalAlignment="Center"/>

        </StackPanel>
    </Grid>
</Window>
