<Window x:Class="Restaurant_Manager.Views.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant_Manager.Views"
        mc:Ignorable="d"
        Title="MenuView" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>

            <TextBlock Text="Меню" FontSize="20" Margin="0,0,0,10"/>
            <ListView ItemsSource="{Binding MenuItems}" Height="200">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="150"/>
                            <TextBlock Text="{Binding Price}" Width="80"/>
                            <TextBlock Text="{Binding Category}" Width="100"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">


                <TextBox Text="{Binding NewItem.Name}" Width="150" Margin="0"/>
                <TextBlock Text="Назва"
               Foreground="Maroon"
               Margin="5,2,0,0"/>
                <TextBox Text="{Binding NewItem.Price}" Width="150" Margin="0"/>
                <TextBlock Text="Ціна"
                Foreground="Maroon"
                Margin="5,2,0,0"/>
                <TextBox Text="{Binding NewItem.Category}" Width="150" Margin="0"/>
                <TextBlock Text="Категорія"
                Foreground="Maroon"
                Margin="5,2,0,0"/>
                <Button Content="Додати" Command="{Binding AddCommand}" Width="80"/>


            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
